## ----setup, include=FALSE-----------------------------------------------------
knitr::opts_chunk$set(collapse = TRUE, comment = "#>", eval = FALSE)
library(RDML7)

## -----------------------------------------------------------------------------
# x <- system.file("extdata/stepone_std.rdml", package="RDML7") |>
#   readRDML()
# 
# rdmlSummary(x)
# validateRDML(x)
# 
# metadata <- asTable(x)
# amp <- getFData(x, dpType = "adp", longTable = TRUE)
# 
# writeRDML(x, "experiment-copy.rdml")

## -----------------------------------------------------------------------------
# x <- setFData(x, fdata, description)

